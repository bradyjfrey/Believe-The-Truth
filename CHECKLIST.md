# Believe the Truth — Master Checklist

The running to-do for the game. Grouped by phase. See `PLAN_character_models.md` and
`PLAN_lobby_spawn.md` for the detailed how-tos behind specific items. **Full character specs —
stats, passives, every ability with keybinds/cooldowns/damage — live in `• specs/`** (the planning
.docx). The abilities section below is the build list distilled from it.

## ✅ Done
- [x] GirlA fully dressed (rig flattened, costume welded, fire VFX, hair) — recipe in `tools/dress-girla.luau`
- [x] Momotaro fully dressed (added missing Head + Neck, mask + hair on head, robe/webbing welded) — recipe in `tools/dress-momotaro.luau`
- [x] Rokurokubi fully done (flattened, faces forward, arms folded + frozen, floats) + **segmented 12-piece neck that waves like a cartoon snake** — `tools/dress-rokurokubi.luau`, `build-neck-chain.luau`, `finalize-rokurokubi.luau`, `rokurokubi-NeckWave.luau` (lives in StarterCharacterScripts)
- [x] **Momotaro's companion bird (Hawk)** — spawns beside him each round, flaps, and follows smoothly. `Momotaro.StartPassives` clones the `Hawk` from `ReplicatedStorage.Companions`, sets a `FollowTarget` ObjectValue = the character, parents it under him. The bird's own script (`tools/companion-bird.server.luau`) just anchors + flaps + tags it `FollowingCompanion`; the per-client `src/StarterPlayer/StarterPlayerScripts/CompanionFollower.client.lua` drives the smooth ride-along + flight-circle cancel on each render loop (server-driven anchored motion stuttered). Tune spot/facing in `CompanionFollower` → `CONFIG.Hawk`. **The dogs will reuse this same follower.**
- [x] Lobby + map spawn flow — hang out in the lobby while waiting; teams split to blue (Warden) / red (Yokai) on round start; back to lobby on round end
- [x] Lobby + in-round music (lobby theme + CODE RED chase theme; chase audio pending Roblox moderation)
- [x] **Killer character-select picker** — "Choose Your Yokai" screen: Grenze Gotisch title, tiled-stripe cards with live 3D `ViewportFrame` models (slow spin, peek above the box), blood-red hover, double border, countdown, locked "COMING SOON" slot. Runtime-built in `src/StarterPlayer/StarterPlayerScripts/CharacterPickerUI.client.lua`; keeps the existing `CharacterPicker` remote contract. Mockup: `mockups/killer-select.html`. Uploaded assets: stripe tile `119070341954890`, lock icon `127360932839161`. Per-character camera knobs live in the `CAMERA` table.
- [x] **Dressed models wired into the round spawn** — on round start each player becomes their real model (Momotaro / GirlA / Rokurokubi); all three spawn, move, and animate. Done via a temp `StarterPlayer.StarterCharacter` swap + `LoadCharacter` (so Roblox injects the R6 `Animate` + StarterCharacterScripts), tagged `Dressed=true` so `Bootstrap` skips the recolor. Toggle per character in `RoundService.DRESSED_ENABLED`; lobby stays normal avatars. (`RoundService.spawnPlayerAt`, `Bootstrap.applyCharacterStats`.)
  - **Model fixes applied (re-run if a re-export drops them):** Rokurokubi's neck `Motor6D`s rebuilt via `tools/rebuild-rokurokubi-neck.luau`; `NeckWave` now waits for the joints to replicate before scanning; Momotaro's root joint renamed `Root Hip`→`RootJoint` and a duplicate costume `Torso` renamed (`tools/fix-momotaro-rig.luau`). These joints/names are invisible in the Explorer unless you expand each part — easy to lose on export.
- [x] **Otohime fully added (2nd survivor) + survivor select screen** — dressed rig (`tools/dress-otohime.luau`) in `CharacterModels`, registered as a Warden, stub ability module, placeholder survivor stats. The picker is now theme-aware: red "CHOOSE YOUR YOKAI" for killers, light-blue "CHOOSE YOUR WARDEN / PROTECT THE TOWN IN" for survivors; every player picks concurrently. Tested 2-player.
- [x] **Camera zoom-out cap** — `player.CameraMaxZoomDistance` set from `Constants.Camera.MaxZoomDistance` (28 studs) on join, so nobody can pull back to a bird's-eye view and scout the map. (Lobby + round.)

## ✅ Done (cont.)
- [x] **Private reveals no longer leak to everyone** — Momotaro's Bird's Eye View + Girl A's Breach highlights used to be created server-side, so the glow showed to ALL players (the revealed enemy saw themselves lit up; teammates got the reveal free). Now the server fires a `ShowHighlight` remote to the ONE player who should see it, and their client draws the glow locally (`HighlightReveal.client.lua`). Rokurokubi's disguise eye-glow is left public on purpose (it's a Warden-catchable "tell"). Tested.

## 🐞 Known issues (check later, not blocking)
- [x] **Jumping — RESOLVED & tested.** All four behave: Momotaro/Otohime/Girl A do a small map-contained hop, Rokurokubi floats (can't jump). Two-part fix: (1) `Bootstrap.applyCharacterStats` forces a known jump state at spawn — `Constants.Jump.Power` (50) default, per-character override **0 for Rokurokubi**; (2) Momotaro's hand-repaired rig *refuses the native Humanoid jump* (grounded, `JumpPower=50`, `Jump=true` received, body movable by raw velocity, but never enters Jumping state — diagnosed via `tools/inspect-jump.luau` + a live state monitor), so `src/StarterPlayer/StarterPlayerScripts/JumpControl.client.lua` is a fallback: on jump request when grounded it gives the native jump 60ms, then applies the upward velocity itself if it didn't fire. Working rigs jump natively first (no double jump); Rokurokubi excluded by her 0 velocity. _(Open curiosity, not blocking: WHY his repaired rig refuses native jump — likely a subtle leftover from the `Root Hip`→`RootJoint` repair.)_

_(Resolved: "dressed Momotaro can't move in lobby" — lobby now spawns normal avatars, dressed only in-round. "Yokai arm freeze" / "Momotaro frozen" — was Momotaro's rig naming (`Root Hip` root joint + duplicate `Torso`), fixed via `tools/fix-momotaro-rig.luau`.)_

## 🔜 Core build (the critical path to "all three characters real")
- [x] **Otohime — 2nd survivor (Warden) — DONE.** Rig + select screen done earlier; now her **abilities are built & tested**: **Healing Pulse** (`E`, +20 HP to nearby allies — NOT herself, she's a team medic; visual sits in front of her), **Dark Moon** (`Q`, slow forward-drifting moon à la Ranni's Dark Moon — first Yokai within the hit radius takes 25). Numbers in `Constants.Otohime` (weren't in the spec; tunable). Inputs bound in `AbilityInput`. Her HP is her own `Constants.Otohime.MaxHealth` (110 placeholder). *(Effect templates `OtohimeHealingPulse` / `OtohimeDarkMoon` wired through the new EffectsService.)*
  - [x] Rig built + dressed (`tools/dress-otohime.luau`): her parts were a uniform 2.4× R6 rig missing its HumanoidRootPart + all 6 joints; script builds the skeleton (joints scaled 2.4×), keeps her native size, welds the kimono by `body_*`/`sleeve_*` name + respects Brady's hand-welded pants. Model deployed to `ReplicatedStorage.CharacterModels.Otohime` (raw rig kept as `OLD Otohime`).
  - [x] Registered in `Types.Character`/`CharacterTeam` (Warden) + `RoundService.DRESSED_ENABLED`; placeholder survivor stats in `Bootstrap` (mirrors Momotaro, no abilities yet).
  - [x] **Survivor select screen:** `CharacterPickerUI` is now theme-aware — the server sends the role and the screen themes red "CHOOSE YOUR YOKAI / KILLER / THE HUNT BEGINS" for killers, **light-blue "CHOOSE YOUR WARDEN / SURVIVOR / PROTECT THE TOWN IN"** for survivors. Keeps the "COMING SOON" locked slot. `RoundService` now lets *every* player pick (Yokai + all Wardens) concurrently instead of hardcoding Momotaro.
  - [x] **TESTED (2-player):** survivor screen shows Momotaro + Otohime in light blue ("CHOOSE YOUR WARDEN / PROTECT THE TOWN IN"), killer screen red as before; both pick correctly and spawn. Otohime portrait was facing away → fixed with `CAMERA.Otohime.face = 180`. *(Optional later: her card framing sits a touch small/low — tune `CAMERA.Otohime` zoom/drop if desired.)* Her two effects (Healing Pulse, Dark Moon) listed under Effects below.
- [x] **Momotaro's companion dogs — Inuta** (*Guard Dog* `E`) — **DONE & tested.** **TWO dogs, both deploy together** as visuals of the ONE ability (the "easy version" the planning doc recommended — resolved the open question in `PLAN_character_models.md`). `Momotaro.GuardDog` spawns both models by their real Studio names (`Constants.GuardDog.DogModelNames` = "Momotaro's larger dog" / "Momotaro's smaller dog"), lines them up at a "guard post" (`DeployForwardStuds` in front, `DogSpacingStuds` apart — larger left, smaller right), faces them his way, and **anchors** them (they stand guard). One shared bark/slow/40HP/60s behavior. **No quadruped rig needed** — anchoring skips physics; the rig only matters if/when we add a walk cycle (still deferred). **Tested 2-player: detection, 50% slow, 1-5 bark damage, and the bark SOUND all work.** Bark sound plays client-side via the new `PlaySound` remote (`SoundPlayer.client.lua`) since server sounds were silent; volume in `Constants.GuardDog.BarkVolume`.
- [~] **Build the Effects** — pipeline DONE, individual effects ongoing:
  - [x] **`EffectsService:Play` is now a real player** (no longer a print stub). Finds a template named like the effect in `ReplicatedStorage.Effects`, clones it, **strips any scripts** (free-asset safety), positions it (handles a Part, Model, *or* Folder), turns on its particle emitters + plays sounds, cleans up after a per-name lifetime. Supports `extras.AttachTo` (weld+ride a limb), `extras.CFrame` (world spot or local offset when attached), `extras.Scale`, `extras.Lifetime`, `extras.Parent`. Effects are PURELY visual — heal/damage stays in ability code.
  - [x] **`GirlASlash`** — done & tuned. Attaches to her Right Arm and rides the swing (offset/rotation/scale tuned in `GirlA.Slash`); the lunge no longer dashes her into it.
  - [x] **Healing Pulse** (`OtohimeHealingPulse`) — wired: plays in front of Otohime on her `E`, centered via EffectsService's bounding-box centering.
  - [x] **Dark Moon** (`OtohimeDarkMoon`) — wired: rides a forward-drifting mover part (EffectsService `AttachTo`), spent on first Yokai hit.
  - [ ] Other per-ability VFX as we get to them (`GirlASlashHit`, Momotaro's, Rokurokubi's…) — just need a matching-named template dropped in the folder.
  - **Convention:** templates live in `ReplicatedStorage.Effects`, named `<Character><Effect>` to match the name the ability passes to `EffectsService:Play`.
- [x] **Build the Weapons — DONE.** GirlA's Hatchet welded to her `Right Arm` (handle→arm, blade→handle, massless/no-collide) and Momotaro's Katana welded to his `Left Arm`, both inside their `CharacterModels`. Code-driven **swing** (no uploaded animation): `WeaponSwing` LocalScript poses the shoulder's `C0` in RenderStepped (beats the default Animate; `Transform` got stomped). Server fires `WeaponSwing` remote from GirlA.Slash (`GirlAHatchet`, two-handed) and Momotaro.Katana (`MomotaroKatana`, left arm). Tunables per style in `WeaponSwing` `CONFIG` (Duration/WindupFrac/WindupAngle/SwingAngle/PrimaryArm/TwoHanded/Axis). ⚠️ Katana `CooldownSeconds` temporarily **0** for the kids to feel out — spec value was **25**.

## ⚔️ Abilities & passives (build list — exact numbers/quotes in `• specs/`)
Code already stubs a lot of this: `AbilityService`, `AbilityModule`, `EffectsService` (e.g. `GirlASlash`),
`Incognito.client`, `DisguiseService`, `BleedService`, and Girl A's Hotspots in `RoundService`.
- **Momotaro** — Warden (Support + Stunner), HP 110. Passive **Bird's Eye View** (Kijiro highlights Yokai ~6.5s every 45s). Abilities: **Katana** `Q` (0.5s windup → 30-stud dash, stun 3s + 15 dmg) · **Guard Dog** `E` (drop Inuta to bark/slow) · **Messy Eater** `R` (Saru's banana peel → slip/ragdoll) · **Kibi Dango** `F` (heal 30 self / 40 ally).
- **Girl A** — Yokai, HP 1700, walk 105% / run 150%. Passive **Auto Connect** (teleport between map Hotspots). Abilities: **Slash** `LMB` (15 dmg) · **Breach of Privacy** `Q` (pop-up reveals a Warden; closing early = 25 dmg) · **Stray Blade** `E`-hold (throw cleaver, 35 dmg + Bleed) · **Incognito Mode** `R` (invisible 8s + Hotspot warp).
- **Rokurokubi** — Yokai, HP 2000, ~10% faster than survivors, free starter. Passive **Hidden Hunger** (stomach-growl audio when she's near). Abilities: **Neck Wrap** `Q` (bind + DoT, button-mash escape) · **Bite** `M1` (bleed, stacks ×3) · **Disguise** `R` (look like a Warden) → **Strangle** `Q` while disguised (choke, disguise drops).

## 🚀 After launch (additions, not for first ship)
- [x] **Messy Eater (`R`) — DONE & tested (peel + Saru).** The peel is the **real banana-peel mesh** (`ReplicatedStorage.Companions.BananaPeel`, Creator Store asset `2795329450`), scaled up (`Constants.MessyEater.BananaPeelScale`) and **grounded** (raycast + `groundOnFloor`). A hidden invisible **trigger pad** (`TriggerPadSize`) does the slip detection. Slip/ragdoll (2s) per spec; peel + pad clean up together.
  - [x] **Saru the monkey model** — the kids built him (27 parts, fully welded, rigged with a HumanoidRootPart + Humanoid; `tools/inspect-saru.luau` verifies the weld graph — only fix needed was anchoring the root). Whole drop now lands `SaruForwardStuds` (8) IN FRONT of Momotaro (at his own spot Saru hid inside his legs and shoved him up), grounded + scaled (`SaruScale` 3) + no-collide; he "eats" for `SaruEatSeconds` (2) then the peel replaces him — matches the spec beat ("gives a banana to Saru, who eats it before throwing the peel"). The floating "Saru" nameplate (from the Humanoid) stays on purpose — fun inside reference. *(A walk/idle animation pass may come later, like the dogs/Hawk.)*

## 🎨 UI & lobby
- [x] **Health bar (HUD)** — bottom-left self health bar in `HUD.client.lua`: a fill bar (length = HP fraction) + a `current / max` number on top. **Colorblind-safe on purpose** — no green→red coding; meaning comes from bar length + the number. Re-binds each spawn (incl. the dressed-model swap); updates on any damage/heal. Roblox's default health bar is disabled so there's only one.
- [x] **No passive health regen** — a `Health` Script in `StarterCharacterScripts` (empty, overrides Roblox's default regen by name). HP now only comes back from real heals (Otohime's Healing Pulse, Momotaro's Kibi Dango); damage sticks. *(Add a slow trickle inside that script later if ever wanted.)*
- [x] **Sprint + stamina** — hold **Shift** to run (per-character sprint speeds; Otohime added to the list). Sprinting drains a stamina pool (`Constants.Stamina`); it refills when you stop, and emptying it drops you back to walk until it recovers `MinToSprint`. Client-side in `Sprint.client.lua`, exposed via a `Stamina` attribute (0..1). HUD shows an amber **stamina bar with a ⚡** floated to the **bottom-right** of the screen (health bar is bottom-left); both same size, themed to match.
- [x] **Ability cooldown indicators (HUD)** — DONE & tested. Bottom-center row above the round timer: one square per ability (key + name); on cooldown it grays out, a dark shade drains away, and the seconds count down. Server stamps `AbilityReadyAt_<Name>` player attributes from `AbilityModule.startCooldown` (`GetServerTimeNow`, no clock drift); HUD counts down locally. Key list now lives in ONE shared module (`Shared/AbilityBindings.lua`) that both `AbilityInput` and the HUD read, so binds and icons can't drift apart (Rokurokubi's Disguise is a `HudOnly` row — DisguisePickerUI owns her R key). *(Visual polish deferred until the kids play with it.)*
- [x] **Lobby whiteboard you can actually "write" on** — DONE & tested. Hold left-click and drag on the kids' board (`Workspace > Maps > Finished Lobby > Finished lobby > Whiteboard`) to draw; hold-E ProximityPrompt wipes it. `WhiteboardService` finds the writing surface automatically (biggest flat face inside the model — no part renames needed), puts a `WhiteboardCanvas` SurfaceGui on it, validates/stores/rebroadcasts strokes, and replays the full board to late joiners (client sends "Ready"). `Whiteboard.client.lua` claims left-click at HIGH ContextActionService priority ONLY when the cursor is on the board in range (sinks it so the camera can't grab the drag; elsewhere clicks pass through). Distance is measured to the board's NEAREST point (its center floats ~23 studs up — center-distance blocked drawing entirely). Marker color auto-assigned per player from `Constants.Whiteboard.MarkerColors`. **Auto-wipes on every round start** (`RoundService._enterRound` → `WhiteboardService:Wipe()`) so no doodle outlives one lobby wait. ⚠️ This board's visible side is the part's BACK face (`Constants.Whiteboard.Face = "Back"`); dots on the Front face render invisibly. All knobs in `Constants.Whiteboard`.
- [ ] *(Way later)* **Skin store** — buy/equip Token & Toastful skins

## ✨ Wishlist / polish (later)
- [ ] **Killer intros (cinematic):**
  - GirlA — emerges from a computer screen: *"Welcome user, to your demise"* (high-quality scene)
  - Rokurokubi — floats/runs at the victim (shown on the killed player's avatar body), suffocates them with her neck after a few seconds of struggle, then charges
- [ ] **Music for the skin store** — the lobby + in-round chase themes are done (see Done above); the skin store still needs its own track. Pull from Brady's Drive folder (see `reference_music_folder` memory). *(Stretch: per-area map themes, Rokurokubi custom theme.)*
- [ ] **Character animations:**
  - GirlA — floats while dragging her hatchet with one free hand; the other hand can attack / help support the hatchet
  - Rokurokubi — neck sways slightly, arms folded, slides/levitates around the map hunting players

## 🛠 Workflow notes
- **No Argon.** Claude edits code on disk; tells Brady exactly which existing scripts to paste into Studio (disk → Studio), then save. New script files = Claude walks Brady through creating the instance.
- **Models/markers/UI** live Studio-side; backed up via `.rbxm` exports + Download-a-Copy place backups.
- Always **commit before** any risky sync.
